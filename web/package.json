{
  "name": "iryouhi-csv-web",
  "private": true,
  "type": "module",
  "scripts": {
    "ssg": "bun run ssg/build.ts",
    "prebuild": "bun run scripts/generate-icons.ts",
    "dev": "vite",
    "build": "vite build && bun run ssg",
    "preview": "vite preview --host"
  },
  "dependencies": {
    "@lit-labs/ssr": "^3.3.1",
    "lit": "^3.1.2"
  },
  "devDependencies": {
    "simple-icons": "^13.9.0",
    "typescript": "^5.5.4",
    "vite": "^5.4.10"
  }
}
